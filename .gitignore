# macOS
.DS_Store
.AppleDouble
.LSOverride

# Xcode user data
xcuserdata/
*.xcuserstate
*.xcscmblueprint

# Build artifacts
*.dmg
*.ipa
*.dSYM.zip
*.dSYM

# DerivedData (lives in ~/Library, not here, but just in case)
DerivedData/

# Build output folders
.build/
.build_release/
*.resolved

# CocoaPods (not used, but safe)
Pods/
Podfile.lock

# Ephemeral session / handoff scratch docs (working notes, not source)
GONE_*HANDOFF*.md
GONE_Sweep_*.md
GONE_REMAINING_PLAN_*.md
